# Particle Life
Currently only avaliable for Apple Silicon. Tested on an M1 with 8 GB ram.

## Installation
Install the julia language.
Clone the repo.
Run `julia`, press `]`, and run `add Metal, WGLMakie, Bonito, Colors`.
Run `julia display.jl` and go to the url printed.
